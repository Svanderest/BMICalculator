<Page
    x:Class="BMICalculator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BMICalculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <RelativePanel MinHeight="300">
        <TextBox Name="txtHeight" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True"></TextBox>
        <TextBlock Name="lblHeight" RelativePanel.LeftOf="txtHeight" RelativePanel.AlignVerticalCenterWith="txtHeight">Height</TextBlock>
        <TextBox Name="txtWeight" RelativePanel.AlignLeftWith="txtHeight" RelativePanel.Below="txtHeight"></TextBox>
        <TextBlock Name="lblWeight" RelativePanel.LeftOf="txtWeight" RelativePanel.AlignVerticalCenterWith="txtWeight">Weight</TextBlock>
        <Button Name="btnCalculate" RelativePanel.AlignLeftWith="lblWeight" RelativePanel.AlignRightWith="txtHeight" RelativePanel.Below="txtWeight" Click="btnCalculate_Click">Calculate</Button>
        <TextBlock Name="lblResult" RelativePanel.Below="btnCalculate" RelativePanel.AlignHorizontalCenterWithPanel="True"></TextBlock>
    </RelativePanel>
</Page>
